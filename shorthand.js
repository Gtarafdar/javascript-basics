//  variable dclaration

var tomatoPrice = 25;

// variable update or set a new value
tomatoPrice = 35;
//  add somthin to the previous value
var price1 = 31;
var price2 = 10;

//  add some value to the existing value
price1 = price1 + 10;

// addition to the same varibale shortahand

// =+ adding some value to the existing value
price1 += 10;

// -= deductine some value to the existing vallue
price2 -= 5;

//  *= multiply value to existing value
price1 *= 5;

//  /= is to devide value from the existing one

price1 /= 5;

// ++ means adding 1 value to existing value

price1++;

// -- means minus 1 value to existing value

price1--;

console.log(price1);
